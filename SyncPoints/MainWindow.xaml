<Window x:Class="SyncPoints.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SyncPoints"
        xmlns:graphsharp="clr-namespace:GraphSharp.Controls;assembly=GraphSharp.Controls"
        xmlns:zoom="clr-namespace:WPFExtensions.Controls;assembly=WPFExtensions"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>
        <zoom:ZoomControl Grid.Row="1">
            <Canvas x:Name="mainPanel" Height="200" Width="200">
                <graphsharp:GraphLayout x:Name="graphLayout" 
                                    Graph="{Binding GraphToVisualize}"
                                    LayoutAlgorithmType="ISOM"
                                    OverlapRemovalAlgorithmType="FSA"
                                    HighlightAlgorithmType="Simple"
                                    
                                    />
            </Canvas>
        </zoom:ZoomControl>
        <Button x:Name="TestDot" Click="TestDot_Click"/>
    </Grid>
</Window>
